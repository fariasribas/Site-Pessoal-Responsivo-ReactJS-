import React from "react";
import "../styles.css"; // Importar o CSS global

const MainSection = () => (
  <main className="main-section">
    <h2>Bem-vindo!</h2>
    <p>Esta é a página principal do meu site.</p>
  </main>
);

export default MainSection;
